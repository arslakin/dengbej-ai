[pytest]
# Each Lambda uses the same filename (lambda_function.py), so tests must run
# per-directory to avoid import collisions.
#
# Recommended command:  python run_tests.py
#
# For a single suite: python -m pytest backend/daily_audio/tests -v

